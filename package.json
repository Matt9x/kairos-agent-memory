{
  "name": "kairos-agent-memory",
  "version": "0.1.0",
  "private": true,
  "description": "Experimental agent memory with confirmation, provenance, invalidation, conservative retrieval and persistence contracts.",
  "type": "module",
  "license": "MIT",
  "engines": { "node": ">=22" },
  "scripts": {
    "demo": "node examples/offline-memory.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:memory": "node --test tests/memory-retrieval.test.mjs",
    "test:persistence": "node --test tests/memory-persistence-adapter.test.mjs"
  }
}
